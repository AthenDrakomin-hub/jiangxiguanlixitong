// Gemini AI Service removed.
// This file is kept as a placeholder to avoid breaking potential imports if any are missed, 
// though all active usages should be removed.

// Use _ prefix to indicate the variable is intentionally unused
export const generateDishDetails = async (_dishName: string) => {
  console.warn("AI features have been disabled.");
  return null;
};