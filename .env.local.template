# Vercel KV 环境变量配置模板
# 注意：在 Vercel 部署时，这些变量会自动注入，无需手动配置

# Vercel KV（Upstash Redis）配置
# 在 Vercel Dashboard 创建 KV 实例后自动生成
KV_REST_API_URL=https://your-instance.upstash.io
KV_REST_API_TOKEN=your_token_here
KV_URL=rediss://default:token@your-instance.upstash.io:6379

# 管理员登录凭证（必需）
# 在 Vercel 项目设置 → Environment Variables 中配置
VITE_ADMIN_USER=admin
VITE_ADMIN_PASS=your_secure_password

# 应用 URL（可选）
# 用于生成二维码的目标链接，留空则自动使用当前域名
# 生产环境示例：https://your-app.vercel.app
VITE_APP_URL=

# 本地开发说明：
# 本项目采用无本地开发模式，所有代码直接推送到 GitHub 由 Vercel 构建部署
# 如需本地测试，可复制此文件为 .env.local 并填入真实值